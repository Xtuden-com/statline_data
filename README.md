# statline_data

## Cloud Functions

A collection of cloud functions deployed on gcp invoked by cloud scheduler periodically throughout the day. 

## data-backend

A simple spring application that sets up routes for Statline frontend to query and display. 
In the theme of deploying serverless applications this will be run using cloud run. Dockerfile to be added later. 